export interface IExp {
  title: string;
  subtitle: string;
  content: string;
}

export interface ISkills {
  icon: string;
  title: string;
  description?: string;
}

export interface IProject {
  title: string;
  image: string;
  description: string;
  url?: string;
}